apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: tempo
  namespace: argocd

spec:
  project: platform

  source:
    repoURL: https://charts.tempo.io
    chart: tempo
    targetRevision: latest
    helm:
      valueFiles:
        - ../releases/tempo/values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
