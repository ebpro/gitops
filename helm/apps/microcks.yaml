apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: microcks
  namespace: argocd

spec:
  project: platform

  source:
    repoURL: https://charts.microcks.io
    chart: microcks
    targetRevision: latest
    helm:
      valueFiles:
        - ../releases/microcks/values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
