apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: loki
  namespace: argocd

spec:
  project: platform

  source:
    repoURL: https://charts.loki.io
    chart: loki
    targetRevision: latest
    helm:
      valueFiles:
        - ../releases/loki/values.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
