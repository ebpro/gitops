# ArgoCD Image Updater bootstrapper Secret
#
# Uncomment below if you want to use an ExternalSecret to override
# the git credentials from Vault instead of the embedded PAT.

# apiVersion: external-secrets.io/v1
# kind: ExternalSecret
# metadata:
#   name: image-updater-git-override
#   namespace: argocd
# spec:
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: vault
#   refreshInterval: 24h
#   target:
#     name: image-updater-creds
#     creationPolicy: Owner
#     deletionPolicy: Retain
#     template:
#       engineVersion: v2
#       mergePolicy: Merge
#       type: Opaque
#       data:
#         password: "{{ .githubToken }}"
#   data:
#     - secretKey: githubToken
#       remoteRef:
#         key: argocd/image-updater-git-creds
#         property: githubToken

---
# TODO: Move to Vault after initial bootstrap:
#   vault kv put secret/argocd/image-updater-git-creds githubToken="<GH_PAT>"
# Then uncomment the ExternalSecret above.
