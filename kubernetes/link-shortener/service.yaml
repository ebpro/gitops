apiVersion: v1
kind: Service
metadata:
  name: link-shortener
  namespace: link-shortener
  labels:
    app.kubernetes.io/name: link-shortener
    app.kubernetes.io/managed-by: quarkus
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app.kubernetes.io/name: link-shortener
