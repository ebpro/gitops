apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: synapse-headers
  namespace: synapse
spec:
  headers:
    customRequestHeaders:
      X-Forwarded-Proto: https
      X-Forwarded-Host: matrix.ebruno.fr
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: synapse-redirect
  namespace: synapse
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: synapse-http-redirect
  namespace: synapse
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`matrix.ebruno.fr`)
      kind: Rule
      middlewares:
        - name: synapse-redirect
      services:
        - name: synapse
          port: 8008
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: synapse-ingress
  namespace: synapse
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`matrix.ebruno.fr`)
      kind: Rule
      middlewares:
        - name: synapse-headers
      services:
        - name: synapse
          port: 8008
  tls:
    certResolver: letsencrypt-ovh
