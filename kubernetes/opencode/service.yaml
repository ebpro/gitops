apiVersion: v1
kind: Service
metadata:
  name: opencode
  namespace: opencode
  labels:
    app.kubernetes.io/name: opencode
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4096
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: opencode
