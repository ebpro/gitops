apiVersion: opentelemetry.io/v1alpha1
kind: Instrumentation
metadata:
  name: sonarqube-otel
  namespace: sonarqube
spec:
  exporter:
    endpoint: http://alloy.alloy.svc.cluster.local:4318
  sampler:
    type: parentbased_traceidratio
    argument: "1"
  propagators:
    - tracecontext
    - baggage
  java:
    resources:
      limits:
        cpu: 200m
        memory: 256Mi
      requests:
        cpu: 50m
        memory: 128Mi
  env:
    - name: OTEL_SERVICE_NAME
      value: sonarqube
    - name: OTEL_RESOURCE_ATTRIBUTES
      value: namespace=sonarqube,cluster=compute-lsis-2
    - name: OTEL_METRICS_EXPORTER
      value: "none"
