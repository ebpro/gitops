apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom
  namespace: kube-system
data:
  gitea-keycloak.override: |
    # Disabled to avoid CoreDNS CrashLoopBackOff due to 'hosts' plugin collision
    # Use pod-level hostAliases instead.
    