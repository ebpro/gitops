apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: argocd-server-oidc-credentials
  namespace: argocd
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  refreshInterval: 24h
  target:
    name: argocd-server-oidc-credentials
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      type: Opaque
      data:
        oidc.clientSecret: '{{ .ARGOCD_SERVER_OIDC_CLIENT_SECRET }}'
        oidcClientSecret: '{{ .ARGOCD_SERVER_OIDC_CLIENT_SECRET }}'
        OIDC_CLIENT_SECRET: '{{ .ARGOCD_SERVER_OIDC_CLIENT_SECRET }}'
        ARGOCD_SERVER_OIDC_CLIENT_SECRET: '{{ .ARGOCD_SERVER_OIDC_CLIENT_SECRET }}'
  data:
  - secretKey: ARGOCD_SERVER_OIDC_CLIENT_SECRET
    remoteRef:
      key: oidc/argocd
      property: clientSecret
