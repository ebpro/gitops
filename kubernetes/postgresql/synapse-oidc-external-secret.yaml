apiVersion: v1
kind: Secret
metadata:
  name: synapse-oidc-secret
  namespace: synapse
type: Opaque
stringData:
  # Generate Keycloak OIDC client 'synapse' in platform realm,
  # then update with the actual secret from Vault.
  SYNAPSE_OIDC_CLIENT_SECRET: PLACEHOLDER_UPDATE_ME
