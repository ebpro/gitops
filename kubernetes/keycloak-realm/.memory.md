(memo)
auth:
  resolved: true
  notes: |-
    The NPE Proxy app was failing with "Client not found." because the Keycloak
    client `clientId: proxy` was used in both sonarqube and nexus ingress routes
    for oauth2-proxy, but the keycloak secret had `clientId: oauth2-proxy`. The
    Træfik forwardAuth middleware on the SonarQube IngressRoute was configured to
    redirect to Keycloak as `client_id=proxy`. After fixing the keycloak client ID
    in Keycloak AND the oath2-proxy secrets to `oauth2-proxy`, both apps now
    authenticate via Keycloak with matching credentials.
  pending:
    - "browser SSO isn't fully tested yet. Cookie refresh cycles and SSO session sharing need verification."
